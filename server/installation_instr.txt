Step 1:
Set your flask app,
 export FLASK_APP=server // if linux
 set FLASK_APP=server // if windows

Step 2:
Run following commands (don't miss the dot),
 pip install -e .
 flask run
 flask run --host=0.0.0.0 (external)

Requirements installed are in the setup.py file