Step 1:
Run following commands to install dependencies,
 pip install -r requirements.txt

Step 2:
 python run.py

// Run it through flask
Step 2:
Set your flask app,
 export FLASK_APP=server // if linux
 set FLASK_APP=server // if windows

 Run the app with:
 flask run